Create a list called "`user_info`".

Then let the user enter the following information in the console:

* First Name
* Last Name
* Age
* Height

All the information above should be stored in the `user_info` list.

Finally, print out the list.
